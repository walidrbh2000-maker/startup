<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: allow cleartext so `flutter run` against a local
     http:// backend works. Replaces the release config for debug builds. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
